---
- name: "extract artifact."
  unarchive:
    src: ~/project/.circleci/ansible/deploy/files
    dest: /home/ubuntu/

- name: "start app."
  become: true
  shell: |
    npm install
    pm2 start npm -- start